# Base URL for the backend API (required)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1

# Optional fallback URLs for local/dev API auto-retry (comma-separated)
# NEXT_PUBLIC_API_FALLBACK_BASE_URLS=https://localhost:62487/api/v1

# Optional configuration for analytics or monitoring
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_ANALYTICS_ID=
